<div class="card w-100">
  <div class="card-body">
    <p>Dear <%= @user.name %>,</p>
    <p>
      Thank you for your payment of <%= number_to_indian_currency(@receipt.total_amount) %>. We have processed your payment and are waiting for it to be cleared.
    </p>
  </div>
</div>
<div class="mt-3"></div>
<%= render "receipt_mailer/receipt", receipt: @receipt %>
