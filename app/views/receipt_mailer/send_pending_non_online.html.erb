<div class="card w-100">
  <div class="card-body">
    <p>Dear <%= @user.name %>,</p>
    <p>Thank you for your payment of <%= number_to_indian_currency(@receipt.total_amount) %>.</p>
  </div>
</div>
<div class="mt-3"></div>
<%= current_client.templates.where(_type: "Template::ReceiptTemplate").first.parsed_content(@receipt) %>
