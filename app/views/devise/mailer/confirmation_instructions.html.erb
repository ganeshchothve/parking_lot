<table cellpadding="0" cellspacing="0" width="100%">
  <tr>
    <td style=" color: #000;font-size: 32px;font-weight: 600;line-height: 35px;vertical-align: bottom;">Confirm Your Account!</td>
  </tr>
  <tr><td>&nbsp;</td></tr>
  <tr>
    <td>Welcome <%=  @resource.name %>,</td>
  </tr>
  <tr><td>&nbsp;</td></tr>
   <tr>
    <td style="line-height:26px;">
      <span style="font-size:15px;line-height:26px;color:#000;font-weight:700;">
      <%if @resource.channel_partner.present? %>
        <span> <%= @resource.channel_partner.name %>,has registered your interest for <%= current_project.name %>.
        Please confirm your account by clicking the link below and book your <%= current_client.name %> Home in 4 easy steps!</span>
      <%else%>
         Thank you for registering at <%= current_project.name %>. Please confirm your account by clicking the link below and book your <%= current_client.name %> in 4 easy steps!</span>
      <%end%>
        <br/>
        <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, channel_partner_id: @resource.channel_partner_id) %></p>
      <%if @resource.channel_partner.present? %>
      <p>If you have any questions please reach out to <%= @resource.channel_partner.name %> at <%= @resource.channel_partner.phone %></p>
      <%end%>
    </td>
  </tr>
</table>
