<div class="card w-100">
  <div class="card-body">
    <p>
      Dear <%= @resource.name %>,
    </p>
    <div class="mb-3"></div>
    <p><%= I18n.t("devise.text_msg.account_locked") %></p>
    <div class="mb-3"></div>
    <%= link_to I18n.t("devise.keys.unlock_my_account"), unlock_url(@resource, unlock_token: @token) %>
  </div>
</div>
