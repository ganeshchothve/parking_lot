<% template = ::Template::BookingDetailFormTemplate.where(name: 'booking_detail_form_pdf').first %>
<% if template.present? %>
  <% begin %>
    <%= ::ERB.new(template.content).result( binding ).html_safe %>
  <% rescue StandardError => e %>
    <%= Rails.logger.error "-------- #{e.message} --------" %>
  <% end %>
<% end %>
