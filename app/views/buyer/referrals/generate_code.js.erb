<% if @user.valid? %>
  $('.usere-referral-code').html('<%= get_referral_code.html_safe %>');
  $('.invite-friend-link').html('<%= invite_friend_link.html_safe %>');
<% else %>
  Amura.global_error_handler( @user.errors.as_json )
<% end %>