<table class='table my-customer-table responsive-tbl'>
  <thead class="th-default">
    <thead>
      <tr class="bg-primary white">
        <th><%= t('mongoid.attributes.campaign_budget.source') %></th>
        <th><%= t('mongoid.attributes.campaign_budget.total_budget') %></th>
      </tr>
    </thead>
  </thead>
  <tbody>
    <td><%= I18n.t("mongoid.attributes.campaign/campaign_sources.#{campaign_budget.source}") %></td>
    <td><%= campaign_budget.total_budget %></td>
  </tbody>
</table>