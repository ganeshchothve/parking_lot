<div class='row mt-4'>
  <div class='col-12'>
    <% template = ::Template::InvoiceTemplate.where(project_id: @invoice.project_id).first %>
    <% if template.present? %>
      <%= ::ERB.new(template.content).result( binding ).html_safe %>
    <% end %>
  </div>
</div>
