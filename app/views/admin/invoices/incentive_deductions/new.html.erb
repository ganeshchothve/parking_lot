<%= nested_form_for(@incentive_deduction, url: admin_invoice_incentive_deductions_path, local: true, remote: true, html: {class: 'modal-remote-form validate-form', "data-resource-type": global_labels['incentive_deductions'], "data-type": "json", id: 'incentive_deduction_form'}) do |f| %>
  <%= render 'form', f: f %>
<% end %>
