<section class="mb-5 mt-3">
  <div class="">
    <div class="row">
      <div class="col-lg-12 col-xs-12 col-md-6 col-sm-12 no-pd">
        <div class="table-btn-set float-end pb-3 text-end mt-1">
          <!--
          <div class="filter-search">
            <input type="text" name="" placeholder="Search projects">
          </div>
          -->
          <div class="filter-popup">
            <a class="db-filter-btn fn-500" href="javascript:;">
              <i class="fa fa-filter fill-theme"></i>
            </a>
          </div>
          <% if policy([:admin, Sms]).sms_pulse? %>
            <%= link_to I18n.t("controller.smses.links.sms_pulse"), sms_pulse_admin_smses_path, class: 'btn-primary border px-3 py-2 border-primary white rounded py-2 ms-2 btn' %>
          <% end %>
        </div>
      </div>
      <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12 no-pd">
        <div class="box-card relative">
          <%= render 'index', {smses: @smses} %>
          <div class="filter-box">
            <%= render 'filters', url: admin_smses_path %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
