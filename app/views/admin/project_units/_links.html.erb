
<%# if policy([:admin, project_unit]).release_unit? %>
  <%# = link_to "Release Unit", release_unit_admin_project_unit_path(project_unit), method: :patch, class: 'dropdown-item' %>
<%# end %>

<% if policy([:admin, project_unit]).show? %>
  <%= link_to "View Details", [:admin, (project_unit.blocked? ? project_unit.booking_detail : project_unit)], class: 'dropdown-item' %>
<% end %>

<% if policy([:admin, project_unit]).edit? %>
  <%= link_to "Edit", edit_admin_project_unit_path(project_unit), class: 'dropdown-item modal-remote-form-link' %>
<% end %>

<% if policy([:admin, project_unit]).quotation? %>
  <%= link_to "Get Quotation", quotation_admin_project_unit_path(project_unit), class: 'dropdown-item' %>
<% end %>
