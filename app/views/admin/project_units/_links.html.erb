
<% if policy([:admin, project_unit]).show? && action_name != 'show' %>
  <%#= link_to t('helpers.show.link_name', model: ProjectUnit.model_name.human), project_unit.booking_detail.present? ? admin_booking_detail_path(project_unit.booking_detail) : admin_project_unit_path(project_unit) , class: 'dropdown-item' %>
<% end %>

<% if policy([:admin, project_unit]).edit? %>
  <%= link_to t('helpers.edit.link_name', model: ProjectUnit.model_name.human), edit_admin_project_unit_path(project_unit), class: 'dropdown-item modal-remote-form-link' %>
<% end %>

<% if policy([:admin, project_unit]).quotation? %>
  <%= link_to t('controller.project_units.quotation.link_name'), quotation_admin_project_unit_path(project_unit), class: 'dropdown-item', target: '_blank' %>
<% end %>

<% if policy([:admin, project_unit]).choose_template_for_print? %>
  <%= link_to t('controller.templates.choose_template_for_print.link_name'), choose_template_for_print_admin_custom_templates_path(subject_class: project_unit.class.model_name.i18n_key.to_s, subject_class_id: project_unit.id.to_s), class: 'dropdown-item modal-remote-form-link' %>
<% end %>
