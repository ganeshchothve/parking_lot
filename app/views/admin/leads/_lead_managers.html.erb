<section class="">
  <div class="">
    <div class="row">
      <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12">
        <div class="box-card relative">
          <%= render 'admin/leads/show_index_lead', lead_managers: lead_managers %>
        </div>
      </div>
    </div>
  </div>
</section>
<%= javascript_tag do %>
  $(document).ready(function(){
    var count = <%= raw lead_managers.count %>
    if(count != 0){
      $('#collapseCpLeadActivities').collapse('show')
    }
  });
<% end %>
