<div class="modal-header">
  <h3 class="title">Reassign Lead</h3>
  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body">
  <div class="mb-3">
    <%= f.label :closing_manager_id, class: 'control-label' %>
    <%= f.select :closing_manager_id, User.where(booking_portal_client_id: current_client.id).sales.available.collect{|user| [(user.ds_name), user.id]}, {prompt: I18n.t("global.select")}, class: 'selectize', required: true %>
  </div>
</div>
<div class="modal-footer text-end">
    <%= f.submit I18n.t("global.submit"), class: "btn btn-primary" %>
</div>
