<% template = ::Template::BookingDetailFormTemplate.where(project_id: @booking_detail.project_id, name: 'booking_detail_form_html').first %>
<% if template.present? %>
  <%= ::ERB.new(template.content).result( binding ).html_safe %>
<% end %>
