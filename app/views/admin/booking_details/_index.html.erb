<% booking_details.each do |booking_detail| %>
<div class="col-lg-12 booking-listing">
  <div class="single-booking-prjt-list mb-4">
    <div class="booking-prjt-thumb">
      <div class="booking-prjt-thumb-inner position-relative">
        <h3 class="title text-center"><%= link_to booking_detail.lead.name, admin_booking_detail_path(booking_detail) %></h3>
      </div>
    </div>
    <div class="booking-inner-info">
      <div class="inner-info position-relative">
        <p class="devl-name"><b>Project Name:</b> <%= booking_detail.project.name %></p>
        <p class="devl-name"><b>Tower Name:</b> <%= booking_detail.project.name %></p>
        <p class="devl-name"><b>Unit Name:</b> <%= booking_detail.booking_project_unit_name %></p>
      </div>
      <div class="booking-prjt-date-info">
        <div class="inner-date">
          <span class="font-small fn-14 mb-0 theme-txt-color mb-0"><i
              class='fa fa-circle status_<%= BookingDetail.human_attribute_name("status.#{booking_detail.status}").downcase.gsub(' ', '_') %>  me-2 fa-sm'></i><%= BookingDetail.human_attribute_name("status.#{booking_detail.status}") %>
          </span>
        </div>
      </div>
      <div class="booking-prjt-stats">
        <div class="prjt-inner-stats">
          <p>
            <%= booking_detail.booked_on ? I18n.l(booking_detail.booked_on) : '-' %>
            <span><%= t('mongoid.attributes.booking_detail.booked_on') %></span>
          </p>
          <p>
            <%= booking_detail.tentative_agreement_date ? I18n.l(booking_detail.tentative_agreement_date) : '-' %>
            <span><%= t('mongoid.attributes.booking_detail.tentative_agreement_date') %></span>
          </p>
          <p>
            <%= booking_detail.agreement_date ? I18n.l(booking_detail.agreement_date) : '-' %>
            <span><%= t('mongoid.attributes.booking_detail.agreement_date') %></span>
          </p>
        </div>
        <div class="booking-prjt-meets">
          <div class="prjt-menu-top float-md-right">
            <div class="dropdown">
              <a href="javascript:void(0);" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-haspopup="true"
                aria-expanded="false">
                <i class="fa fa-ellipsis-v" aria-hidden="true"></i></a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                <% if booking_detail.project_unit.present? %>
                  <%= render "#{current_user_role_group}/booking_details/links", booking_detail: booking_detail, link_name: '' %>
                <% else %>
                  <%= render "#{current_user_role_group}/booking_details/booking_without_inventory_links", booking_detail: booking_detail, link_name: '' %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>
<!----------- Pagination ---------->
<div class="col-lg-12 launchpad_inventory_pagination">
  <div class="row py-3 bg-white mb-2 shadow-sm ms-0">
    <div class="col-lg-6 pt-2">
      <p class="table-entry mb-0"><%= page_entries_info booking_details %></p>
    </div>
    <div class="col-lg-6 table-pagination">
      <%= will_paginate booking_details, list_classes: %w(pagination justify-content-end) %>
    </div>
  </div>
</div>