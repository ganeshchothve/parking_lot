<section class="mb-5 mt-3">
  <div class="">
    <div class="row">
      <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12 no-pd">
        <div class="table-btn-set float-end pb-3 text-end">
          <!--
          <div class="filter-search">
            <input type="text" name="" placeholder="Search Email">
          </div>
          -->
          <div class="filter-popup">
            <a class="db-filter-btn fn-500" href="javascript:;">
              <i class="fa fa-filter fill-theme"></i>
            </a>
          </div>
          <% if policy([:admin, Email]).monthly_count? %>
          <%= link_to I18n.t("controller.emails.links.monthly_count"), monthly_count_admin_emails_path, class: ' theme-btn btn btn-primary white ms-1 d-inline-block' %>
          <% end %>
        </div>
      </div>
      <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12 no-pd">
        <div class="box-card relative">
          <%= render 'index', {emails: @emails} %>
          <div class="filter-box">
            <%= render 'filters', url: admin_emails_path %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
