<header>
  <nav class="navbar navbar-expand-lg bg-white navbar-light sticky-top fixed-top">
    <div class="container no-pd">
    <div class="header-logo">
      <%= link_to (current_client.logo.present? ? (image_tag current_client.logo.url, class: 'img-fluid position-absolute start-50 top-50 translate-middle', alt: "Logo") : "launchpad-logo.png"), home_path(current_user), class: 'navbar-brand me-0'%>
      </div>
      <% unless user_signed_in? %>  
      <div class="text-end">
        <div class="user-info float-end hide-xs">
          <div class="d-inline-block">
            <%= link_to 'T & C', dashboard_terms_and_condition_path, active: :exclusive, class: 'p-style', target: "_blank" %>
          </div>
        </div>
        <div class="user-info login-btn float-end hide-xs">
          <div class="d-inline-block">
            <img src="<%= asset_path 'lock-black.svg' %>" alt="User Info">
            <%= link_to "Log in", new_session_path("user"), class: "p-style" %>
          </div>
        </div>
        <div class="dropdown hide-xs float-end register-dropdown">
          <div class="d-inline-block">
            <a class="dropdown-toggle p-style" href="javascript:;" id="navbarDropdown" role="button"
              data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Register</a>
            <div class="dropdown-menu menu-top mobile-home-menu" aria-labelledby="navbarDropdown">
              <%= link_to "Register as Channel Partner", new_channel_partner_path, class: "dropdown-item btn-as-channel-partner text-start" %>
              <%= link_to "Register as Customer", register_path, class: "btn btn-primary btn-as-customer-partner text-start" %>
            </div>
          </div>
        </div>
        <div class="d-inline-block">
          <%= link_to new_channel_partner_path, class: "reg-chnl-prtnr-btn show-xs p-style" do %>
          <%= image_tag 'user-alt-black.svg', alt: "User Info" %>
          Register as Channel Partner
          <% end %>
        </div>
        <div class="d-inline-block">
          <%= link_to "Register Now", register_path, class: "btn register-btn bg-primary show-xs p-style ms-3 px-3" %>
        </div>
        <div class="user-info login-btn d-inline-block show-xs mt-2">
          <div class="d-inline-block">
            <img src="<%= asset_path 'lock-black.svg' %>" alt="User Info">
            <%= link_to "Log in", new_session_path("user"), class: "p-style primary" %>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </nav>
</header>
