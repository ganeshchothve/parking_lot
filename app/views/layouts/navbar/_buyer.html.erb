    <% if policy(:dashboard).index? %>
      <li class="nav-item">
        <%= active_link_to t('controller.dashboard.index.link_name'), dashboard_path, active: :exclusive, class: "nav-link" %>
      </li>
    <% end %>

    <% if policy([:buyer, BookingDetail.new(user: current_user, lead: current_user.selected_lead, project_unit: ProjectUnit.new(status: 'available', user: current_user, lead: current_user.selected_lead))]).hold? %>
      <li class="nav-item">
        <%= active_link_to t('controller.searches.towers.link_name'), new_search_path, active: :inclusive, class: "nav-link" %>
      </li>
    <% end %>

    <% if false #policy([:buyer, Receipt.new(user: current_user)]).new? %>
      <li class="nav-item">
        <%= active_link_to t('controller.receipts.new.link_name'), new_buyer_receipt_path, active: :inclusive, class: 'nav-link modal-remote-form-link' %>
      </li>
    <% end %>

    <% if false #policy([:buyer, BookingDetail]).index? %>
      <li class="nav-item">
        <%= active_link_to t('controller.booking_details.index.link_name'), buyer_booking_details_path, active: :inclusive, class: "nav-link" %>
      </li>
    <% end %>

    <% if false #policy([:buyer, Receipt]).index? %>
      <li class="nav-item">
        <%= active_link_to t('controller.receipts.index.link_name'), buyer_receipts_path, active: :inclusive, class: "nav-link" %>
      </li>
    <% end %>

    <%= render 'layouts/navbar/custom_links' %>