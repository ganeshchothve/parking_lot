<section class="first-sec welcome-box my-3">
  <div class="px-4">
    <div class="row">
      <% template = ::Template::UITemplate.where(booking_portal_client_id: current_client.try(:id), name: 'dashboard/index/_admin').first %>
      <% if template.present? %>
        <%= get_view_erb(template) %>
      <% end %>
    </div>
  </div>
</section>
<% if DashboardPolicy.new(current_user, :dashboard).admin_dashboard? %>
  <%= render 'dashboard/index/crm' %>
<% end %>
