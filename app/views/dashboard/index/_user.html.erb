<%= render 'users/welcome' %>
<div class="mt-5 mb-5">
  <div class="px-4">
<!-- Projects -->
<%= render 'project_units/section' %>
<!-- My Projects -->
<div class='no-pd'>
  <!-- <div class="separate-line container"></div> -->
  <%#= render 'users/my_projects' %>
  <!-- Receipts -->
  <!-- <div class="separate-line container"></div> -->
  <%= render 'receipts/section', receipts: current_project.present? ? current_user.receipts.where(project_id: current_project.id).paginate(page: params[:page] || 1, per_page: params[:per_page]) : current_user.receipts.paginate(page: params[:page] || 1, per_page: params[:per_page]) %>
  <!-- Time slots -->
  <!-- <div class="separate-line container"></div> -->
  <% if false #policy([:buyer, TimeSlot]).index? %>
    <%#= render 'users/time_slots' %>
  <% end %>
</div>
<!--
<div class="separate-line container"></div>
<section class="user-db-sec-5 device-pd pb-4">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 no-pd">
        <div class="row">
          <%#= render 'dashboard/index/partials/collaterals' %>
        </div>
      </div>
    </div>
  </div>
</section>
<div class="separate-line container"></div>
-->
<div class='mb-5 pb-5'></div>

  </div>
</div>